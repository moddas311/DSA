#include <bits/stdc++.h>
using namespace std;
int main()
{
    int n;
    cin >> n;
    int i, j, k = 0;
    for (int i = n / 2; i <= n; i++) // loop e jodi / or * ei typer operation thake taile tar complexity log(n).
    {
        for (int j = 2; j <= n; j * 2)
        {
            k = k + n / 2;
        }
        }
    return 0;
}